# Local dev: point at your local Postgres
DATABASE_URL=postgres://postgres:password@127.0.0.1:5432/fit4830

# Production (DigitalOcean Managed Postgres) requires TLS. App Platform
# injects this for you as ${db.DATABASE_URL} - see .do/app.yaml.
# DATABASE_URL=postgres://user:pass@host:25060/db?sslmode=require
